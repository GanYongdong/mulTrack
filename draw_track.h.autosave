#ifndef DRAW_TRACK_H
#define DRAW_TRACK_H

#include <iostream>
#include <opencv2/opencv.hpp>

class draw_track
{
public:
    draw_track();
    
    cv::Mat track_update(cv::Mat img, cv::Point point);
    

private:
    std::vector<cv::Point> pointVec;
};

#endif // DRAW_TRACK_H
